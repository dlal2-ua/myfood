# packages/shared

Tipos y esquemas compartidos entre `apps/api` y `apps/web` (generados desde el OpenAPI de FastAPI). Vacío hasta que haya suficiente superficie de API para que generar tipos merezca la pena — se añade a partir de la Fase 1.
